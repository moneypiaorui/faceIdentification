PCA
LDA
LPP
isomap
SwissRoll
这几个函数已经打包好了，放到项目文件夹下即可直接调用
PCA传入参数（训练集），返回排好序的特征向量
LDA传入参数（训练集，训练集标签，组数），返回排好序的特征向量
LPP传入参数（训练集，高斯核函数的sigma），返回排好序的特征向量
PCA传入参数（训练集），返回排好序的特征向量
isomap传入参数（训练集(按行放智)，KNN的邻居数k，目标维度d），返回（降维后的数据Y（按列放置），最短路径矩阵D）
SwissRoll无参数，返回（瑞士卷数据集X，rgb矩阵 tt）